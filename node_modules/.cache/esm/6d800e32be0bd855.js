_be0‍.x([["default",()=>Context]]);class Context {
    constructor({ state = {}, observers = [] } = {}) {
        this.state = state;
        this.observers = observers;

        return this;
    }

    add(...observers) {
        this.observers = new Set([
            ...this.observers,
            ...observers,
        ]);

        return this.observers;
    }
    remove(...observers) {
        for(let observer of observers) {
            this.observers.delete(observer);
        }

        return this.observers;
    }
};