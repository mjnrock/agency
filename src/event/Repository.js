/**
 * This is basically a <Map> and a <Registry> combined, where the
 * 	@key is a *unique* << any >>, and the @value is the state of that
 * 	thing.  The main purpose is to create a centralized state-management
 * 	context when an individual object does not contain local state.
 */
export class Repository {
	constructor() {
		
	}
};

export default Repository;